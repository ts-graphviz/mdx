import { Renderer } from './renderer';

export { Components, Plugin } from './types';
export { GraphvizProps, Graphviz } from './components/Graphviz';
export { Renderer };

export default new Renderer();
